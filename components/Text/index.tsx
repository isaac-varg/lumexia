import LabelDataPair from "./LabelDataPair";
import PageTitle from "./PageTitle";
import SectionTitle from "./SectionTitle";


const Text = {
    LabelDataPair: LabelDataPair,
    PageTitle: PageTitle,
    SectionTitle: SectionTitle,
}

export default Text;